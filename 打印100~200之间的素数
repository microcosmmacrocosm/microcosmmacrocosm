#include<stdio.h>
int main()

{
	int i = 0;
	int a = 2;
	for (i = 100; i < 200; i++)
	{
		for (a=2; a < i; a++)
		{
			if (i % a == 0)
				break;
			else
				i = i + 0;
			
			
		}
		if (a==i)
		printf("%d",i);
		
			
		}
	
	return 0;
}
